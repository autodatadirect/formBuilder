/**
 * Testing popOver 
 */

describe('An popOver', function(){
	xit('can be created', function(){
		var input = $('<input type="text" class = "tooltip"/>').wrap('<div/>').inputField();
		
		expect(input.children().attr('class')).toBe('');
	});

	xit('can set what it is going to be appended to', function(){

	}); 

	xit('can initialize itself', function(){

	});

	xit('can be toggled', function(){

	});

	xit('can show itself', function(){

	});

	xit('can hide itself', function(){

	});

	xit('can set its position', function(){

	});

	xit('can destroy itself',function(){

	});
}); // End of the first describe