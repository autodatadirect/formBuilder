/**
 * Testing select data type 
 */

 describe('A select data type', function(){
 	xit('can be created', function(){
		// There is no create function but I am assuming that we can still test that it has been created
	});

	describe('can be set up', function(){

	});

	describe('can detect when an event will be fired on key down', function(){

	});

	xit('can detect when an event will be fired on key up', function(){

	});

	xit('can handle when an item has been selected', function(){
		// _setSelected
	});

	xit('can select the first non-empty option', function(){

	});

	xit('can be set', function(){

	});

	xit('can remove a selection', function(){

	});

	xit('can handle when an item that has been selected is not found', function(){

	});

	xit('can set a default item', function(){

	}); 

	xit('can select the first item', function(){

	});

	xit('can show or hide a command', function(){

	}); 

	xit('can clear a selected item', function(){

	});

	xit('can set two values to be equal to each other', function(){

	}); 

	xit('can set its label', function(){

	}); 

	xit('can get its label', function(){

	}); 

	xit('can toggle', function(){
		// Not sure what is being toggled
	});

	xit('can get its options', function(){

	}); 

	describe('can filter its options', function(){
		// _filterOptions & _filterOptionsWork, not sure what the latter is doing 
	}); 

	describe('can filter the item selected', function(){
		// Not sure that the 'item' in _filterItem has actually been selected
		// _filterItem
		// _itemShouldBeFiltered
	}); 

	xit('can clear the filter', function(){

	});

	xit('can build an empty option', function(){

	}); 

	xit('can build options', function(){

	}); 

	xit('can sort', function(){

	}); 

	xit('can scroll', function(){

	});

	xit('can open a panel', function(){

	}); 

	xit('can render a label', function(){

	}); 

	xit('can render an item', function(){

	}); 

	xit('can close a panel', function(){

	}); 

	xit('can be updated', function(){

	}); 

	xit('can remove an invalid entry', function(){

	}); 

	xit('can load its values', function(){
		// returns a sorted array of objects that have value and label properties
	});

	xit('can create a map', function(){

	});

	xit('can de-map itself', function(){

	}); 

	describe('can be converted', function(){
		xit('by changing the values when they are entered into the field', function(){
			// toField
		});

		xit('by changing the values when they are taken from the field', function(){
			// fromField
		});
	});


 }); // End of the first describe